server:
  port: 12000

spring:
  application:
    name: GraduateManagementSystem
  profiles:
    active: dev

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl